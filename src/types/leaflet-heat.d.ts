declare module 'leaflet.heat' {
  // This module is a side-effect import that extends Leaflet's L namespace
  // with the heatLayer function. The actual types are declared in HeatmapLayer.tsx
  // using module augmentation.
}
